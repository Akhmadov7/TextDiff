# TextDiff

Сервис статистического сравнения текстовых корпусов. Специализация темы курса №5
(«Статистический анализ загружаемого набора данных») на текстовых данных.

## Что считаем

Пользователь загружает CSV с текстами, разбитыми на две группы (например, отзывы
«позитивные»/«негативные», тексты двух авторов и т.п.). Ядро строит по каждой группе
лексические статистики (длина текста, лексическое разнообразие TTR, средняя длина слова,
частоты слов) и проверяет гипотезу о различии групп статистическим тестом
(Манна-Уитни или t-тест). На межсессии 1 добавляются регрессия по закону Ципфа и
попарное сравнение текстов по TF-IDF.

## Вход / выход

- **Вход:** CSV (колонки `text`, `group` — имена настраиваются в форме), выбор теста.
- **Выход:** таблица статистик по группам, значение теста и p-value, топ частых слов.

## Быстрый старт

```
python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env        # Windows
# cp .env.example .env        # Linux/macOS
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

http://127.0.0.1:8000 — список задач; `/tasks/new/` — форма; `/api/docs` — Swagger UI.

Проверки: `pytest -q`, `pytest core -q`, `ruff check .`, `python -m core example_input.json`.

## Структура

```
core/        ядро (без Django): run(params) -> dict, CLI, тесты на эталонах
web/         модель Task, формы, views, services.py
api/         REST API (Django Ninja): создать/статус/результат/список задач
templates/   форма загрузки, список задач, страница результата
docs/        architecture.md, adr/ (решения), er-diagram.md
```


## Вклад

- Ахмадов Магомед — вычислительное ядро и тесты.
- Зайналабдиев Рамзан — backend и работа с задачами.
- Джамиев Амин — интерфейс и документация.
- Темирсултанов Магомед — API и OpenAPI.



# Предположительные ошибки:
# Разбор fat_integrate

1. Вычислительный алгоритм находится непосредственно во view.
   Вычисления должны находиться в core/.

2. Долгий расчёт выполняется синхронно внутри HTTP-запроса.
   Длительные задачи должны выполняться через очередь и worker.

3. View напрямую работает с Task и базой данных.
   Работа с задачами и их статусами должна находиться в services.py.

4. Используется eval() для обработки пользовательского ввода.
   Значения должны проходить безопасную валидацию без выполнения произвольного кода.

5. Входные параметры a, b и n разбираются непосредственно во view.
   Их следует валидировать отдельной схемой до запуска расчёта.

Исправленные ошибки:
1. core/solver.py — при полностью одинаковых текстах в группах scipy возвращал NaN вместо числа. NaN — не валидный JSON, SQLite отклонял запись в БД ошибкой IntegrityError.
2. web/views.py — неиспользуемый import json и слишком длинная строка (132 символа при лимите 120) — ruff их не пропускал.
3. tests/test_scenario.py — неправильный порядок импортов (изза этого ruff).


